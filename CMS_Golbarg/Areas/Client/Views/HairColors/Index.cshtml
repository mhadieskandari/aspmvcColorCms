@model IEnumerable<CMS_Golbarg.Areas.Client.Models.HairColor>
@using CMS_Golbarg.Helpers
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.InterNationalColorName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InterNationalColorCode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HairPic)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.InterNationalColorName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InterNationalColorCode)
            </td>
            <td>
                @HtmlExtensions.Image(item.HairPic,"imgHairColor",new { @width = "100px", @height = "100px"})

            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }

    </table>*@
<div class="card_content">
    @foreach (var item in Model)
    {
        @Html.Partial("~/Areas/Client/Views/Shared/_HairColorCardView.cshtml", item)
    }
</div>
