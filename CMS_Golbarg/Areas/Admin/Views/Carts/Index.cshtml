@model IEnumerable<CMS_Golbarg.Areas.Admin.Models.Cart>

@{
    ViewBag.Title = "لیست خرید ها";

}


<a class="btn btn-sm fa fa-plus-square " href="@Url.Action("Create","Carts")"></a>


<div class="box">
    <div class="box-header  with-border">
        <h3 class="box-title">لیست خرید ها</h3>
    </div>
    <div class="box-body">



        <table id="cart-list" class="table table-striped table-bordered" cellspacing="0">
            <thead>


                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.PayCoin.NumberOfCoins)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Mixer.Mix)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.PayCoin.Pay.TransitionOfBankNumber)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.RegisterDate)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ConfirmDate)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.EndDate)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.StartDay)
                    </th>

                    <th>

                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        
                        <td>
                            @Html.DisplayFor(modelItem => item.PayCoin.NumberOfCoins)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Mixer.Mix)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.PayCoin.Pay.TransitionOfBankNumber)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.RegisterDate)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ConfirmDate)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.EndDate)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.StartDay)
                        </td>
                        <td>
                            <a href="@Url.Action("Edit")" class="btn btn-success"><i class="fa fa-edit"> ویرایش</i></a>
                            <a href="@Url.Action("Details")" class="btn btn-info"><i class="fa fa-info"> جزئیات</i></a>
                            <a href="@Url.Action("Delete")" class="btn btn-danger"><i class="fa fa-remove"> حذف</i></a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>


    </div>

</div>


@section scripts{
    @Scripts.Render("~/bundles/adminlte-rtl_dataTables")
    <script>
        $(document).ready(function () {
            $('#cart-list').DataTable();
        });

        $.extend($.fn.dataTable.defaults, {
            searching: true,
            ordering: true
        });
    </script>


}


