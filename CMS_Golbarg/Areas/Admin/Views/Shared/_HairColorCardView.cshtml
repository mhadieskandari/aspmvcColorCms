@model CMS_Golbarg.Areas.Admin.Models.HairColor
@using CMS_Golbarg.Helpers
<div class="card">
    @Html.ImageFor(model => model.HairPic, new { @class = "card_img"})
       
        @Html.HiddenFor(model=>model.Id)
    <div class="card-detail">
        @*@if (Model.getInfoInternational != null)
            {*@
        <div class="card_details">
            @Html.DisplayFor(model => model.getInfoInternational)
        </div>
        @*}*@

        @*@if (Model.getInfoPersian != null) {*@
        <div class="card_details">
            @Html.DisplayFor(model => model.getInfoPersian)

        </div>
        @*}*@
    </div>
        

            <div class="form-group">
                <div class="col-md-6">
                    <a href="@Url.Action("Edit",new { id = Model.Id })" class="btn btn-transparent"><small><span class="glyphicon glyphicon-edit"></span></small></a>

                </div>
                <div class="col-md-6">
                    <a href="@Url.Action("Delete", new { id = Model.Id })" class="btn btn-transparent"><small><span class="glyphicon glyphicon-trash"></span></small></a>
                </div>
            </div>



    </div>
